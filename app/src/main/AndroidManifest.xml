<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.navigationdraweractivity">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:fullBackupContent="true"
        android:icon="@mipmap/ic_krushi_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_krushi_logo"
        android:supportsRtl="true"
        android:theme="@style/Theme.NavigationDrawerActivity.NoActionBar"
        tools:ignore="AllowBackup">
        <activity android:name=".MainActivity"/>
        <activity android:name=".ui.Cart.CartActivity" />
        <activity android:name="ProfileActivity" />
        <activity android:name=".ui.buy.BuyFullDetailActivity" />
        <activity android:name=".SellProductFullDetailActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".RegistrationActivity" />
        <activity
            android:name=".SplashActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.NavigationDrawerActivity.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>